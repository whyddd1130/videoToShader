# 7月14日项目进度汇报

## 一、项目目标

当前项目目标是构建一个“视频特效到 shader”的闭环生成系统：

```text
输入原始图 + 目标特效视频
-> 分析目标视频相对原始图的视觉变化
-> 检索库侧相似 shader / 特效源码
-> 生成第一版 GLSL shader
-> 渲染 candidate 视频
-> 大模型审查 target 与 candidate 的差异
-> 根据审查结果继续改写 shader
-> 固定轮数后输出所有中间结果，并给出大模型评分最高版本
```

当前阶段重点不是追求某个像素差数值最小，而是验证整条路径是否可行：

```text
目标视频理解
-> 库侧参考选择
-> shader 初稿生成
-> candidate 渲染
-> 大模型差异审查
-> shader 迭代优化
```

## 二、当前主流程

主流程代码位于：

```text
effect_ir_pipeline/effect_ir/closed_loop_shader_iter.py
```

现在一次完整运行包含以下步骤。

### 2.1 输入形式改造

之前系统默认：

```text
目标视频第一帧 = 原始图
```

现在已经改为显式输入：

```text
原始图 image + 目标视频 video
```

命令示例：

```bash
python -m effect_ir_pipeline.effect_ir.closed_loop_shader_iter \
  target.mp4 \
  --input-image source.png \
  --repo-root . \
  --work-dir effect_ir_pipeline/reports/closed_loop_run \
  --max-iters 5
```

当前行为：

- target IR 使用 `--input-image` 作为 reference。
- candidate IR 也使用同一张 `--input-image` 作为 reference。
- Shader Lab 渲染时输入图也是 `--input-image`。
- prompt 中已从旧的 `reference first frame` 改为 `provided input image / source image`。
- 如果不传 `--input-image`，为了兼容旧命令，仍会回退到“视频第一帧作为原图”的旧逻辑。

这个改动解决了一个关键问题：后续如果目标视频第一帧已经包含特效，或者目标视频不是从原图第 0 帧开始变化，系统仍然能正确理解“原图 -> 目标视频”的变化关系。

### 2.2 目标视频生成结构化 IR

系统先根据原始图与目标视频生成 `target_ir`。

IR 描述的是视觉变化机制，而不是画面内容。主要字段包括：

```text
region_scope
geometry_ops
appearance_ops
temporal_pattern
mask_dependency
strength
motion_hint
color_hint
edge_hint
shape_hint
subject_hint
geometry_pattern
mask_shape_source
summary
```

例如，一个波浪形变视频可能被识别为：

```text
geometry_ops = warp
temporal_pattern = periodic
motion_hint = deform
geometry_pattern = directional
shape_hint = line_band
```

### 2.3 库侧 shader 检索

系统用 `target_ir` 与库侧 `library_structured_ir_llm.jsonl` 做相似度匹配，选出相似特效。

当前默认仍使用 top1 作为第一轮 shader 生成的参考：

```text
target_ir -> library_structured_ir_llm.jsonl -> selected_effect -> selected_sources
```

第一轮 shader 不是直接复制库侧代码，而是：

```text
大模型参考 selected_effect 的源码/语义
结合 target_ir
重新生成 shader-lab 可运行 GLSL
```

### 2.4 渲染 candidate 视频

每轮生成以下文件：

```text
iter_01_shader.md
iter_01.vert.glsl
iter_01.frag.glsl
iter_01_rendered.mp4
...
iter_05_shader.md
iter_05.vert.glsl
iter_05.frag.glsl
iter_05_rendered.mp4
```

shader 仍然要求：

```text
uProgress = 0 时输出原图
```

且 fragment shader 只允许使用：

```text
uniform sampler2D inputImageTexture
uniform float uProgress
uniform float uTime
```

### 2.5 大模型视觉审查

每轮 candidate 渲染成功后，系统会构造 contact sheet 给大模型审查。

contact sheet 结构：

```text
TARGET | CANDIDATE | DIFF
```

每一行对应一个相同相对进度：

```text
0.00
0.25
0.50
0.75
1.00
```

其中：

- `TARGET` 是目标视频帧。
- `CANDIDATE` 是当前 shader 渲染结果。
- `DIFF` 是两者绝对差异的可视化图，放大 3 倍，仅作为大模型视觉证据。

注意：`DIFF` 不作为像素评分，也不作为停止条件。

### 2.6 结构化 primitive_edit_plan

为了避免大模型自由发挥，当前要求它输出结构化修改计划。

当前 primitive 词表：

```text
geometry:
identity, translate, scale_x, scale_y, rotate, flip_x, flip_y,
crop_or_pad, perspective_skew, radial_swirl, wave_warp

appearance:
brightness, contrast, saturation, rgb_shift, blur, sharpen, vignette

temporal_curves:
linear, ease_in, ease_out, ease_in_out, bell,
early_progressive, late_progressive, two_phase
```

大模型输出的核心字段：

```json
{
  "visual_difference_summary": "...",
  "candidate_errors": ["..."],
  "optimization_plan": ["..."],
  "primitive_edit_plan": {
    "selected_primitives": [],
    "rejected_primitives": [],
    "implementation_notes": []
  },
  "operation_family": "...",
  "change_family_allowed": false
}
```

下一轮 shader 生成会优先依据 `selected_primitives` 和 `implementation_notes`。

## 三、最新新增：大模型评分机制

之前为了避免误导，已经移除了基于绝对像素差的停止条件和 best 选择。

现在新增的是另一种评分方式：

```text
由大模型基于视觉证据对每一轮 candidate 进行主观匹配评分
```

这不是像素距离，也不是提前停止阈值。

### 3.1 评分字段

每轮大模型审查时，会输出：

```json
{
  "match_score": {
    "overall": 0,
    "geometry": 0,
    "appearance": 0,
    "temporal": 0,
    "mask": null,
    "reason": "中文评分理由"
  }
}
```

含义：

- `overall`：整体匹配度，0-100。
- `geometry`：几何形变、位移、缩放、旋转、翻转等匹配度。
- `appearance`：颜色、亮度、饱和度、模糊、光效等匹配度。
- `temporal`：时间曲线、变化节奏、出现时机匹配度。
- `mask`：局部区域、遮罩、主体区域匹配度；不适用时可为 null。
- `reason`：扣分原因和评分依据。

### 3.2 评分的用途

评分现在有两个用途：

1. 对所有轮次排序。
2. 指导下一轮 shader 优化。

固定轮数结束后，结果 JSON 中会新增：

```json
{
  "score_ranking": [],
  "best_iteration_by_llm_score": {}
}
```

其中：

- `final_*` 仍表示最后一轮。
- `best_iteration_by_llm_score` 表示大模型评分最高的一轮。

这可以解决之前用户肉眼观察到的问题：有时第 2 或第 3 轮比最后一轮更接近目标，但系统没有机制指出中间产物哪一轮更好。

### 3.3 与旧数值差异的区别

当前评分不是：

```text
绝对像素差
阈值停止条件
自动回退机制
```

当前评分是：

```text
大模型基于 contact sheet、IR、当前 shader 的主观视觉评审
```

循环仍然固定执行 `--max-iters` 指定轮数，默认 5 轮，不会因为分数高低提前停止。

## 四、已完成的重要工程改动

### 4.1 固定轮数迭代

当前闭环固定执行：

```text
--max-iters 5
```

不再使用：

```text
差异值 < 0.04 退出
像素距离排序
历史 best 回退
```

### 4.2 每轮结果全部输出

现在不只输出最后一轮，而是每一轮都输出：

```text
iter_01_rendered.mp4
iter_01_shader.md
iter_02_rendered.mp4
iter_02_shader.md
...
iter_05_rendered.mp4
iter_05_shader.md
```

最终结果中：

```text
iteration_outputs
```

会列出所有轮次。

### 4.3 shader 生成失败重试

之前出现过黑屏视频，原因是大模型生成的 shader 源码不完整，例如只生成了一半。

现在增加了校验：

- 检查 vertex / fragment 是否都有代码块。
- 检查是否包含 `void main`。
- 检查括号是否平衡。
- 检查 vertex shader 是否声明 `attribute vec2 position`。
- 检查 fragment shader 是否包含必要 uniform。
- 禁止额外 unsupported uniform。

如果 shader 不完整，会触发重试。

### 4.4 渲染视频有效性检查

现在会检查渲染结果是否：

- 文件存在。
- 可读。
- 有可采样帧。
- 不是全黑 / 空白视频。

如果失败，则当前尝试不计入迭代轮数。

### 4.5 大模型差异审查接入

当前大模型不是只用 IR 文本判断，而是会看到：

```text
contact sheet
target_ir
candidate_ir
current_shader
primitive_vocabulary
```

大模型负责：

```text
指出 candidate 与 target 的差异
给出 primitive_edit_plan
给出 match_score
给出下一轮优化方向
```

## 五、库侧分析与优化方向

### 5.1 库侧分类设计

已经整理了库侧 shader 分类设计文档：

```text
effect_ir_pipeline/library_shader_bucket_design.md
```

推荐将库侧 shader 分为：

```text
geometry_warp
transform_transition
color_tone
blur_bokeh
light_glow_flash
pixel_style
edge_outline
mask_composite
subject_face
```

其中 `subject_face` 是特殊 gate 类，不建议默认参与所有查询。

### 5.2 库侧原子化建议

已经整理根目录文档：

```text
库侧特效原子化建议.md
```

核心结论：

如果后续要从“全库 top1”升级为“多 bucket 多 shader 参考”，库侧特效需要更原子化。

推荐优先补充的 10 个 primitive：

```text
scale_x
scale_y
flip_x
flip_y
translate_x
translate_y
brightness_only
contrast_only
saturation_only
rgb_split_only
```

这些 primitive 对第一轮 shader 稳定性提升最直接，尤其能避免简单拉伸/翻转目标被误识别成复杂 warp、blur 或 wave。

### 5.3 无明显变化特效筛查

检查了 92 个库侧特效在 model 输入上的输出视频。

确认以下特效与 `Nothing__model.mp4` 完全一致，视频 MD5 相同，在当前 model 输入上没有产生可见变化：

```text
BasicBlocks
BrightFlash
FaceLocation
HeadMatting
Illusion
Lut
Residual
RightAlpha
SEFaceSmooth
SpillSuppressor
```

此外：

```text
Nothing
TestPass
```

也不应参与普通默认检索。

建议：

- 不立刻删除源码。
- 默认从普通 model 检索中排除。
- 对人脸、matting、alpha、LUT 等条件触发效果，后续放入条件专用库。

## 六、近期实验情况

### 6.1 test5 / test6 / test7 / test8 固定 5 轮测试

已经使用多个测试视频跑过固定 5 轮闭环。

整体观察：

- 失败重试机制能拦住黑屏或不完整 shader。
- 大模型能较稳定地给出部分 primitive 方向，例如 `scale_x + contrast + saturation`、`wave_warp + rgb_shift + blur` 等。
- 但后续轮次有时会停滞，即视频输出从某轮开始不再明显变化。
- 对几何类目标，模型容易在 `wave_warp`、`translate`、`crop_or_pad`、`radial_swirl` 等操作之间漂移。

### 6.2 Bokeh leave-one-out 实验

实验目标：

```text
输入库中的 Bokeh__model.mp4
但检索时排除 Bokeh 自己
观察系统是否能不参考原始 Bokeh shader 还原效果
```

排除 `Bokeh` 后 top 结果：

```text
DiamondBokeh
MipmapBlur
HexagonalBokeh
SurfaceBlur
PolygonBokeh
```

实际初始参考：

```text
DiamondBokeh
```

结论：

- 系统能找到同族 blur/bokeh 参考。
- 初始方向基本正确。
- 后续模型持续建议 `blur + brightness`。
- 但第 2 轮后输出变化不明显，说明“审查方向正确”和“shader 修改实际生效”之间仍有断层。

### 6.3 WaveWarp leave-one-out 实验

实验目标：

```text
输入库中的 WaveWarp__model.mp4
但检索时排除 WaveWarp 自己
观察系统是否能不参考原始 WaveWarp shader 还原几何形变
```

排除 `WaveWarp` 后 top 结果：

```text
Bowknot
edgeStretch
hahajing
Tornado
BezierWarp
Inhalation
Fold
CCCylinder
```

实际初始参考：

```text
Bowknot
```

结论：

- 系统能找到相邻几何形变家族。
- 第一轮能够生成明显形变。
- 但后续视觉审查发生漂移，第 3 轮开始模型认为 candidate 是错误的全局 wave warp，转而建议 `crop_or_pad + translate`。
- 这说明如果库侧 shader 不够原子化，模型容易在相近但不同的几何操作族之间摇摆。

## 七、当前主要问题

### 7.1 库侧 top1 参考不够稳定

当前第一轮只参考 top1 shader。

问题：

- 如果 top1 是组合效果，模型容易学到无关逻辑。
- 如果目标同时包含多个变化，例如“拉伸 + 翻转 + 颜色变化”，单个 top1 不够覆盖。
- 如果库中没有对应原子 primitive，模型容易用复杂 warp 代替简单 transform。

下一步方向：

```text
多 bucket 检索
每个 bucket 取 top1/top2
参考多个 shader 源码
让模型提取各自 primitive 后再生成第一轮 shader
```

### 7.2 库侧特效不够原子化

很多库侧效果是组合效果，例如：

```text
NeonArc
Metalman
MosaicColor1
CCPageTurnWithBg
```

这些作为业务效果有价值，但作为模型参考源码会带来噪声。

下一步应增加：

```text
atomicity = atomic / composite / conditional
bucket
primitives
source_quality
requires
```

### 7.3 大模型审查与 shader 修改之间存在断层

有时模型能指出正确问题，但生成的下一轮 shader 没有明显变化，或没有按计划实现。

可能原因：

- prompt 对“必须修改哪些代码路径”约束还不够强。
- primitive 到 GLSL 的映射不够模板化。
- 模型参考旧 shader 时容易保留原有错误结构。
- 没有单独的“代码执行后自检”机制来确认目标 primitive 是否真的被实现。

### 7.4 几何类效果容易漂移

几何类目标中，模型容易混淆：

```text
wave_warp
radial_swirl
translate
crop_or_pad
scale_x / scale_y
flip_x / flip_y
```

这也是为什么需要补充更基础的几何 primitive shader，例如：

```text
scale_x
scale_y
flip_x
flip_y
translate_x
translate_y
wave_warp_only
directional_wave_x
directional_wave_y
```

## 八、下一步计划

### 8.1 实现多 bucket 检索

基于已有分类文档，实现：

```text
target_ir -> bucket activation -> each bucket top1/top2 -> multiple reference shaders
```

目标是让第一轮 shader 同时参考多个原子维度，而不是只依赖一个 top1。

### 8.2 新增原子 shader 库

优先补充：

```text
scale_x
scale_y
flip_x
flip_y
translate_x
translate_y
brightness_only
contrast_only
saturation_only
rgb_split_only
```

然后继续补充：

```text
wave_warp_only
radial_swirl_only
gaussian_blur_small
edge_detect_sobel
left_to_right_wipe
center_circle_reveal
```

### 8.3 引入 primitive 实现模板

目前大模型自由写 GLSL，容易漏掉或写偏。

后续可以把常见 primitive 写成模板，让模型只选择和组合：

```text
scale_x -> UV x 方向缩放模板
flip_x -> UV x 翻转模板
rgb_shift -> RGB 通道偏移模板
wave_warp -> 正弦波 UV 偏移模板
brightness -> color 加权模板
```

这样可以提高生成稳定性。

### 8.4 使用大模型评分做实验分析

新增的 `match_score` 可以用于：

- 判断哪一轮最好。
- 分析哪些维度长期低分。
- 发现模型是否在几何、颜色或时序上持续失败。
- 为后续 prompt 和 primitive 库优化提供依据。

后续可以对多组测试视频统计：

```text
best_iteration 分布
geometry/appearance/temporal 分项低分原因
不同 bucket 初始参考对最终得分的影响
```

## 九、当前代码验证情况

最近一次改动后，已执行：

```bash
python -m py_compile ...
python -m unittest discover -s effect_ir_pipeline/tests -v
```

测试结果：

```text
Ran 8 tests
OK
```

当前测试覆盖：

- contact sheet 可解码。
- shader 不完整检测。
- 黑屏视频检测。
- 大模型 visual feedback prompt 不再包含旧 numeric distance。
- 无几何目标时 guardrail 会阻止新增几何 primitive。
- 独立 visual distance 工具的基础行为。

## 十、总结

当前系统已经从早期的“数值差异阈值迭代”转为更符合目标的“大模型视觉审查闭环”：

```text
原始图 + 目标视频输入
固定轮数迭代
每轮输出视频和 shader
失败生成不计数重试
大模型基于 contact sheet 审查差异
大模型输出 primitive_edit_plan
大模型输出 match_score
最终给出所有轮次排序和最高分版本
```

目前验证结果说明：

- 整条闭环路径已经可以跑通。
- 大模型能够识别部分核心差异并给出优化方向。
- 系统已具备失败重试和中间产物保留能力。
- 新增评分机制可以帮助判断中间哪一轮更好。

但要进一步提升还原质量，下一阶段重点应放在：

```text
库侧原子化
多 bucket 多 shader 参考
primitive 到 GLSL 的模板化实现
几何类效果的稳定识别与约束
```

