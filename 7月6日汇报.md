# 7月6日汇报

## 一、项目目标

最终目标：

```text
输入原始图片 + 该图片经过某一视频特效渲染后的视频
逆向推理出该特效对应的 shader 源码
```

当前阶段先做第一步：

```text
输入原始图 + 视频关键帧
从库中识别最可能使用的已有特效
```

## 二、关键约束

实际部署时，库侧只有：

```text
特效代码、filter.json、参数定义等静态信息
```

库侧没有：

```text
该特效在同输入图上的渲染视频
```

因此，正式方案不能依赖库模板视频。此前 `visual_template_score` 虽然效果最好，但只能作为离线实验上限，不能作为最终部署方案。

## 三、数据情况

当前数据来自：

```text
datasets/effect_training/single_shader_multi
```

数据统计：

| 项目 | 数量 |
|---|---:|
| 总样本数 | 576 |
| 成功渲染样本数 | 552 |
| 失败样本数 | 24 |
| 可用特效数 | 92 |
| 每个特效输入变体 | 6 |

当前图像条件检索主要使用 5 个输入变体：

```text
ring_mask, rg_split, cross_lines, gray_ramp, color_gradient
```

## 四、早期训练尝试

早期尝试过直接分类训练：

| 方案 | 输入 | Test Top-1 | Test Top-5 |
|---|---|---:|---:|
| baseline | 视频帧 + 图片 + 参数文本 | 0.00% | 8.93% |
| no_frame0 | 修改采样策略 | 0.00% | 3.57% |
| no_param_text | 去掉参数文本 | 0.00% | 5.36% |

这些结果说明：

1. 直接分类在当前数据规模下效果较差。
2. 参数文本在最终任务中不可用。
3. 仅靠监督分类不利于后续 shader 生成。

因此方向转为：

```text
构造 IR 中间表示，再做库中特效检索。
```

## 五、IR 检索方案

正式可部署方案：

```text
库侧：代码 -> IR
查询侧：原始图 + 视频关键帧 -> IR
检索：查询 IR vs 库 IR
```

IR 主要描述：

| 字段 | 说明 |
|---|---|
| `summary` | 一句话描述视觉变化 |
| `ops` | 主要操作，例如 blur、distortion、mask_matte |
| `attributes` | 连续强度，例如颜色变化、模糊、畸变、运动 |
| `temporal` | 时序模式 |
| `tags` | 关键视觉概念 |

相似度由三部分组成：

```python
score = (
    0.40 * structured_score
    + 0.40 * signature_score
    + 0.20 * diagnostic_score
)
```

| 分数 | 作用 |
|---|---|
| `structured_score` | 比较完整 IR 结构 |
| `signature_score` | 比较抽象变化机制 |
| `diagnostic_score` | 比较 bokeh、sphere、page_turn 等诊断词 |

## 六、评测结果

92 样本结果：

| 方法 | 是否可部署 | Top-1 | Top-5 |
|---|---|---:|---:|
| 旧版 IR 相似度 | 是 | 15.22% | 29.35% |
| 当前可部署 IR 检索 | 是 | 14.13% | 36.96% |
| IR + 视觉模板重排 | 否，仅离线上限 | 30.43% | 56.52% |

结果解释：

```text
当前可部署 IR 检索提升了 Top-5，说明召回能力增强；
但 Top-1 未提升，说明同家族特效之间的精排仍然不足。
```

视觉模板重排能显著提高 Top-1，但它依赖库侧视频，不符合实际约束，因此不能作为最终系统。

## 七、当前结论

当前最重要的结论是：

```text
IR 方案是可部署方向；
视觉模板重排只能作为离线上限，用来证明“视觉变化模式”对精排很重要。
```

正式方案接下来不应继续依赖库侧视频，而应把这部分判别能力迁移到：

```text
更好的代码侧 IR
更好的查询侧 IR
更好的 IR 相似度学习
```

## 八、下一步方向

优先方向：

| 方向 | 说明 |
|---|---|
| 优化代码侧 IR 生成 | 从 Lua/filter.json 更准确提取视觉机制、时序、遮罩、坐标变换 |
| 优化查询侧 IR 生成 | 让模型稳定描述相对原图变化，而不是描述输入图内容 |
| 学习式相似度 | 用现有样本学习 IR 字段权重 |
| 强化同家族区分 | 针对 bokeh、warp、mask/wipe、3D/lens 等类别增加细粒度概念 |
| 引入视觉模型辅助 IR | 用视觉模型帮助生成查询 IR，但不与库视频做模板比较 |

最终路线：

```text
第一步：输入图 + 视频 -> 生成查询 IR
第二步：库代码 -> 生成库 IR
第三步：查询 IR 与库 IR 检索匹配
第四步：基于检索结果和 IR，进一步生成 shader 源码
```
