# 7月15日项目进度汇报

## 今日目标

今天主要围绕“视频到 shader 的闭环迭代”继续优化。重点不再是单纯让差异数值下降，而是让流程更接近真实可用的生成链路：

```text
输入原图 + 目标视频
→ 生成/匹配初始方向
→ 渲染候选视频
→ 大模型直接审查目标视频和候选视频
→ 输出更清晰的修改意见
→ code 模型或 builder 生成下一轮 shader
→ 固定轮数迭代
```

## 一、将 shader 生成改为 edit plan + builder/code model 模式

之前的主要问题是：视觉模型虽然能指出差异，但每轮直接让它重写完整 shader，容易出现方向漂移、代码不完整、黑屏、或者只是在错误方向上微调。

今天将主流程改为：

```text
LLM 看图判断差异
→ 输出 primitive/edit plan
→ 程序化 shader builder 生成稳定 draft
→ 可选 code 模型根据 edit plan 和 draft 优化 shader
→ 渲染
→ LLM 审查
→ 下一轮继续
```

新增/修改内容：

- 新增 `effect_ir_pipeline/effect_ir/shader_builder.py`
  - 支持从 `primitive_edit_plan` 生成稳定 GLSL；
  - 支持 pixelation、translate、flip、scale、blur、rgb_shift、brightness/contrast 等基础组合；
  - 支持 `code_model` backend：先由程序化 builder 生成 draft，再交给代码能力更强的模型优化。
- 主闭环 `closed_loop_shader_iter.py` 已接入该 builder。
- 预留参数：

```bash
--shader-builder-backend programmatic
--shader-builder-backend code_model
--code-model <model-id>
```

今天实际接入的 code 模型为：

```text
ep-9cg1p3-1782977436110826484
```

调用地址使用可访问的办公网 HTTPS：

```text
https://wanqing-api.corp.kuaishou.com/api/gateway/v1/messages
```

## 二、放宽 code 模型修改能力

初版 code-model prompt 过于保守，倾向于“保留 programmatic draft，只做局部 patch”，导致后续改动幅度不够，容易在错误方向上原地打转。

今天将 prompt 改为：

- programmatic draft 只是可运行起点，不是必须保留的模板；
- 如果反馈说明当前方向、特效家族、几何结构或时序明显错误，允许大幅重构 fragment shader；
- code 模型优先参考视觉反馈中的普通画面描述，而不是机械翻译 primitive 术语。

## 三、删除 `uProgress=0` 必须等于原图的限制

之前 shader prompt 中有硬性约束：

```text
uProgress = 0 时必须输出原图
```

这不再适合当前设定。现在输入已经显式提供原图，目标视频第一帧不一定等于原图，可能已经包含特效状态。

今天已删除该限制：

- repair prompt 删除该要求；
- code-model prompt 删除该要求；
- direct-video prompt 删除该要求；
- programmatic builder 不再默认强制 p=0 回到原图；
- 仅当 IR/反馈明确表示“从原图开始”时，builder 才会从原图开始混合。

同时更新了 `visual_distance.py` 中旧的第一帧排除假设，诊断采样现在可包含第一帧。

## 四、严重不匹配逻辑改为 direct reset

之前逻辑：

```text
连续严重不匹配
→ 排除当前库侧 top1
→ 重新检索新的 top1 shader
```

今天改为：

```text
连续严重不匹配
→ 不再重新选择库侧 top1
→ direct reset
→ 直接把原图 + 目标视频采样帧交给 code 模型
→ 生成新的第一版 shader
→ 重新跑完整固定轮数
```

这样可以避免库侧 top1 在错误方向之间反复切换。

新增记录字段：

```json
direct_reset_events
```

默认最多触发一次 direct reset，可通过参数调整：

```bash
--max-direct-resets 2
```

## 五、优化视觉审查 prompt：从术语驱动改为画面描述驱动

之前审查结果过于冗长，且专业术语过多，例如：

```text
pixelation / displacement / geometry family / primitive mapping
```

这些信息虽然结构化，但不够像人类在看视频，容易导致 code 模型围绕术语原地打转。

今天将视觉审查 prompt 改为优先输出：

```json
{
  "target_video_observation": "目标视频画面发生了什么",
  "candidate_video_observation": "候选视频画面发生了什么",
  "most_important_gap": "最关键的一个差距",
  "next_change_in_plain_words": ["下一轮应该把画面改成什么样"]
}
```

要求：

- 少用专业术语；
- 不复述 IR 标签；
- 优先描述画面现象；
- 最多指出 2 个关键差异；
- 最多给 3 条下一轮修改建议。

`primitive_edit_plan` 仍保留，但定位变成“代码落地接口”，不再作为审查结果的唯一核心。

## 六、评分机制改为整体人类主观相似度

之前评分字段包含：

```json
geometry
appearance
temporal
mask
overall
```

实际表现发现，这会让模型像在填技术表格，而不是像人类一样判断“两个视频第一眼像不像”。

今天彻底删除旧分项，新的 `match_score` 只保留：

```json
{
  "overall": 0,
  "reason": "整体像不像的主观理由"
}
```

新的评分原则：

```text
0-20：几乎完全不像，或主效果缺失
21-40：只沾到一点方向，但第一眼不像
41-60：能看出同一大类，但关键细节明显错
61-80：整体方向接近，但参数/节奏有差距
81-100：人眼看起来已经很像
```

并加入约束：

- 如果第一眼不是同一种效果，overall 不要超过 40；
- 如果目标主效果缺失，overall 不要超过 30；
- 如果只是标签相似但画面不像，不要给高分。

对应地，严重不匹配判断也删除了对 `geometry_score` 的依赖，主要依据：

- `overall <= 35`
- 文本中出现“第一眼不像 / 核心效果缺失 / 完全不像”等信号；
- rejected primitives；
- 是否明确要求 change family。

## 七、稳定性修复

今天还修复了几个流程稳定性问题：

1. 主闭环自动补 GLSL precision 头

   部分 code 模型生成的 shader 没有：

   ```glsl
   precision highp float;
   ```

   Shader Lab 不一定报错，但会渲染黑屏。现在主闭环会自动补：

   ```glsl
   #ifdef GL_FRAGMENT_PRECISION_HIGH
   precision highp float;
   #else
   precision mediump float;
   #endif
   ```

2. code 模型无 text 返回时不再直接崩溃

   `/messages` 接口有时只返回 thinking，没有 text。现在 code-model backend 会 fallback 到 programmatic draft，继续流程。

3. direct reset 阶段增加 fallback

   direct reset 初始生成如果连续失败，会 fallback 到 programmatic builder，避免整个流程直接中断。

## 八、主要实验结果

### 1. test9：code model + builder + direct reset + 人类整体评分

结果目录：

```text
effect_ir_pipeline/reports/closed_loop_test9_model_builder_code_human_score_direct_reset_5iter_20260715
```

流程：

- 初始 top1：`DBSnowGlitch`
- 前两轮评分均为 35，触发 direct reset；
- reset 后重新跑 5 轮。

reset 后结果：

| 轮次 | 分数 | 主要评价 |
|---:|---:|---|
| 1 | 45 | 都是横向故障，但目标是细腻扫描线，候选是粗糙马赛克 |
| 2 | 35 | 候选更像柔和液化/模糊，第一眼不像 |
| 3 | 35 | 都有像素化，但目标是特定扫描线/条纹故障，候选只是普通马赛克 |
| 4 | 45 | 都有横向干扰，但目标是硬切数字信号，候选是软扭模拟信号 |
| 5 | 35 | 候选是软边模拟失真，目标是硬边几何故障 |

观察：

- 新评分明显更接近人类“第一眼像不像”；
- 不再因为“都有像素化/横向干扰”而给高分；
- 但生成结果仍没有稳定抓住 test9 的硬边扫描线/块状横向故障结构。

### 2. test4：direct reset 逻辑验证

结果目录：

```text
effect_ir_pipeline/reports/closed_loop_test4_model_builder_code_direct_reset_5iter_20260715
```

流程：

- 初始 top1：`MosaicGlass2`
- 前两轮严重不匹配，触发 direct reset；
- 没有重新选择新的 top1；
- 直接根据原图 + 目标视频采样生成新初版；
- reset 后跑满 5 轮。

结果：

| 轮次 | 分数 |
|---:|---:|
| 1 | 15 |
| 2 | 30 |
| 3 | 35 |
| 4 | 10 |
| 5 | 15 |

观察：

- direct reset 机制已跑通；
- 但 test4 的目标效果仍未被充分还原，说明直接看图生成初版 shader 的能力还不稳定；
- 视觉审查能指出“漩涡/强扭曲/信号故障”等差异，但 code 模型生成仍容易变成普通旋转、马赛克或弱扭曲。

## 九、当前问题

1. 视觉审查比之前更清楚，但 code 生成仍不稳定

   模型能更准确地指出“第一眼不像”的原因，但 shader 生成仍可能走到错误的视觉家族。

2. direct reset 能避免错误 top1 继续影响，但不能保证新初版正确

   直接看原图+目标视频生成 shader 的初始能力还需要增强。

3. primitive/builder 的表达能力仍不够

   对硬边扫描线、块状横向切片、强烈离散 glitch、漩涡扭曲等效果，当前 builder 的原子能力还不够丰富。

4. code 模型输出格式和平台兼容性仍需防护

   虽然已经加了 repair、precision 补全、fallback，但仍会出现无 text、格式不完整、黑屏等情况。

## 十、下一步建议

1. 补充更具体的原子 shader primitive

   重点补：

   - scanline slice shift；
   - hard horizontal block displacement；
   - discrete glitch bands；
   - swirl / vortex warp；
   - RGB channel slicing；
   - fine grid / dense stripe flicker。

2. direct reset 时不要完全自由生成

   可以让模型先输出“目标画面描述 + 关键视觉动作”，再由 builder/code 模型生成 shader，而不是直接看图输出完整源码。

3. 为不同视觉家族建立更强模板

   例如：

   - 硬边数字故障模板；
   - 模拟信号扭曲模板；
   - 漩涡/旋转扭曲模板；
   - 马赛克/像素块模板；
   - 翻转/拉伸/方向模糊模板。

4. 继续减少 prompt 中的术语诱导

   审查模型应更多描述画面，code 模型再负责把画面目标翻译成 shader 结构。
