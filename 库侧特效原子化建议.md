# 库侧特效原子化建议

本文档记录当前库侧 92 个特效在“多分类、多 shader 参考生成第一轮 code”方向上的整理建议。

目标不是简单删除现有库，而是把库侧特效从“完整业务效果集合”逐步整理成更适合检索和组合参考的“视觉 primitive 库”。

## 背景

当前流程中，目标视频会先生成 structured IR，然后在库侧所有 shader IR 中检索一个最相似的效果，第一轮 shader 生成只参考这个 top1 效果的源码。

后续希望优化为：

1. 先把库侧 shader 分成多个视觉操作类别。
2. 在多个类别内分别找到最相似的 shader。
3. 把多个 shader 源码共同交给模型参考。
4. 让模型生成第一轮 shader。

这种方式能覆盖目标视频里的多个视觉维度，例如：

- 几何拉伸 + 翻转
- 颜色偏移 + 对比度增强
- 模糊 + 光效
- 遮罩 reveal + 局部形变

但这也要求库侧 shader 更加原子化。否则模型同时参考多个“组合特效”时，很容易被无关逻辑带偏。

## 推荐库侧分层

建议把库侧特效分成三层：

```text
原子参考 shader：优先参与多 bucket 检索
组合效果 shader：可以参与，但权重降低，只提取局部思路
无效/弱变化 shader：默认排除或条件启用
```

## 当前适合作为原子参考的效果

下面这些效果相对更适合作为第一轮生成的参考源码，因为它们视觉语义较单一。

### 几何类

```text
BezierWarp
WaveWarp
Bulge
Twirl
LensDistortion2
OpticsCompensation
DisplacementMap
Transform
CCPageTurn2
Fold
edgeStretch
Inhalation
```

### 颜色类

```text
ColorBalanceHLS
Lumetri
Invert
ExtractChannel
ShiftChannels
FourColorGradient
FlashWarning
LeaveColor
HairDyeing
```

### 模糊类

```text
MipmapBlur
SurfaceBlur
motionBlur
Bokeh
DiamondBokeh
HexagonalBokeh
PolygonBokeh
```

### 像素 / 马赛克类

```text
LowPixel
MosaicHexagon
MosaicBuilding
Newsprint
AsciiArtStyle
Pattaizer
```

### 边缘 / 描边类

```text
Sketch
EmbossNew
Engrave
Outline
Stroke
LayerOutline
```

### 遮罩 / 合成类

```text
GradientWipe
LayerMask
Progress
RandomImage
pointNine
```

### 光效类

```text
BlingBling
CCLightSweep
EdgeGlow
NeonArc
```

## 当前偏组合效果的特效

下面这些效果不是没用，但往往包含多个视觉因素。作为全局 top1 检索结果可以参考，但在“多 shader 共同参考”时容易引入噪声。

建议标记为：

```json
{
  "atomicity": "composite"
}
```

并在 prompt 中要求模型只提取与 `target_ir` 对齐的局部 primitive，不允许整体模仿。

```text
CCPageTurn
CCPageTurnWithBg
CCSphere
JellyDistortion
kaleida
Mercury
Metalman
MosaicBurr
MosaicColor1
MosaicColor2
NeonArc
NewShatter
SlicingSlide
Shatter
Tornado
Guoqing2021
hahajing
```

举例：

- `NeonArc` 同时包含 displacement、warp、light、channel shift、edge outline。
- `Metalman` 同时包含调色、边缘增强、线性运动。
- `MosaicColor1` 同时包含 displacement、pixelation、channel shift。
- `CCPageTurnWithBg` 同时包含翻页、合成、光效。

因此它们适合作为组合案例，不适合作为原子 primitive 直接参与第一轮生成。

## model 输入上无明显变化的特效

检查范围：

```text
videos/*__model/*__model.mp4
```

检查方式：

- 以 `videos/Nothing__model/Nothing__model.mp4` 作为无效果基准。
- 对 92 个 model 版本视频进行采样比较。
- 对低差异候选生成 contact sheet 进行人工确认。
- 对完全无变化候选进一步计算 mp4 文件 MD5。

确认以下特效与 `Nothing__model.mp4` 完全一致，视频文件 MD5 相同，在当前 model 输入上没有产生可见变化：

```text
BasicBlocks
BrightFlash
FaceLocation
HeadMatting
Illusion
Lut
Residual
RightAlpha
SEFaceSmooth
SpillSuppressor
```

此外：

```text
Nothing
```

本身就是无效果基准。

`TestPass` 也应视作测试/占位效果，不建议参与默认检索。

### 无变化效果的处理建议

不建议立刻删除源码，因为部分效果可能是条件触发型。例如：

- 人脸相关效果可能依赖人脸关键点、分割或特定参数。
- matting / alpha 效果可能依赖额外素材或 mask。
- LUT 类效果可能当前参数为空或未加载。

建议处理方式：

```text
默认不参与普通 model bucket 检索；
只有当目标 IR 或输入条件满足时再启用。
```

例如：

```text
FaceLocation
HeadMatting
SEFaceSmooth
SpillSuppressor
```

可以放入人脸 / 分割 / 条件触发专用库，但不要参与普通视频特效检索。

## 当前变化较弱但不应归为无变化的效果

下面这些效果虽然变化较弱，但通过 diff 能看到局部变化，因此不应放入“无变化”列表：

```text
LayerOutline
LeaveColor
Wiggle
HeadTrack
```

其中 `Wiggle` 和 `HeadTrack` 的变化尤其弱，肉眼可能不明显，但与 `Nothing` 对比后仍能看到边缘、边框或轻微位移差异。

## 当前最缺的原子 shader 类型

如果后续要让多 shader 参考更加稳定，建议补充一批人工构造的原子 shader。它们不一定要来自真实业务效果，可以作为库侧 primitive reference。

### 基础几何 primitive

当前几何类很多是复杂 warp，但缺少干净的基础变换。

建议补充：

```text
scale_x
scale_y
uniform_scale
translate_x
translate_y
rotate_small
flip_x
flip_y
center_zoom_in
center_zoom_out
crop_reveal
```

特别是：

```text
flip_x
flip_y
scale_x
scale_y
```

这些对拉伸、翻转、缩放类目标非常重要。

### 基础颜色 primitive

建议补充：

```text
brightness_only
contrast_only
saturation_only
hue_shift_only
temperature_warm
temperature_cool
rgb_split_only
red_channel_boost
blue_channel_boost
grayscale_fade
```

这样模型在生成第一轮时，不需要从复杂调色 shader 里猜哪部分是亮度、哪部分是饱和度。

### 基础时序 primitive

建议补充：

```text
linear_progress
ease_in_progress
ease_out_progress
ease_in_out_progress
pulse_sin
flicker_random
delayed_start
pingpong_progress
```

很多目标视频不是效果类型错，而是出现时机、强度曲线错。时序 primitive 对闭环优化尤其重要。

### 基础 mask primitive

建议补充：

```text
left_to_right_wipe
right_to_left_wipe
top_to_bottom_wipe
center_circle_reveal
center_circle_hide
soft_radial_mask
hard_radial_mask
line_band_mask
ring_mask
```

### 基础 blur primitive

建议补充：

```text
gaussian_blur_small
gaussian_blur_large
directional_blur_x
directional_blur_y
radial_blur
zoom_blur
edge_preserve_blur
```

### 基础边缘 primitive

建议补充：

```text
edge_detect_sobel
outline_black
outline_white
edge_glow_only
emboss_only
sharpen_only
```

### 基础像素化 primitive

建议补充：

```text
square_pixelate
hex_pixelate
circle_dot_halftone
ascii_like_luma_blocks
posterize_color
limited_palette
```

## 最优先补充的 10 个 primitive

如果只先补最有收益的一小批，建议优先补：

```text
scale_x
scale_y
flip_x
flip_y
translate_x
translate_y
brightness_only
contrast_only
saturation_only
rgb_split_only
```

原因：

1. 它们覆盖最常见的视频变化。
2. 它们非常原子，模型容易复用。
3. 它们能避免模型把简单目标误解成复杂效果。
4. 它们对第一轮 shader 质量提升最直接。

## 建议新增的库侧元信息

后续每个库侧效果可以不只保存 `structured_ir`，还可以增加：

```json
{
  "effect_name": "scale_x",
  "bucket": "geometry_warp",
  "atomicity": "atomic",
  "primitives": ["scale_x"],
  "requires": [],
  "avoid_when": [],
  "source_quality": "good"
}
```

组合效果示例：

```json
{
  "effect_name": "NeonArc",
  "bucket": "light_glow_flash",
  "atomicity": "composite",
  "primitives": ["edge_glow", "channel_shift", "displacement"],
  "source_quality": "mixed"
}
```

条件触发或当前 model 上无变化的效果示例：

```json
{
  "effect_name": "HeadMatting",
  "bucket": "subject_face",
  "atomicity": "conditional",
  "requires": ["subject_segmentation"],
  "source_quality": "inactive_on_model"
}
```

## 后续实施建议

建议按以下顺序推进：

1. 保留现有 92 个效果，不立即删除源码。
2. 给现有库增加 `bucket / atomicity / primitives / source_quality` 元信息。
3. 把无变化效果从默认普通检索中排除。
4. 把组合效果降权，只作为局部 primitive 参考。
5. 新增一批人工原子 shader。
6. 多 bucket 检索时优先选 `atomicity=atomic` 且 `source_quality=good` 的 shader。
7. 只有在目标 IR 明确需要时，才启用 `subject_face`、`mask_composite` 等条件类 bucket。

这样可以从“业务特效库”逐步过渡到“可组合的 shader primitive 库”，更适合大模型参考多个源码生成第一轮 shader。

