# 7月13日汇报

## 一、当前目标

当前目标是实现一个不依赖数值评估的 shader 闭环迭代流程：

```text
输入目标视频
-> 检索库中相近特效
-> 生成第一版 shader
-> 渲染候选视频
-> 由大模型审查目标视频与候选视频的视觉差异
-> 根据大模型反馈生成下一轮 shader
-> 固定迭代 5 轮后停止
```

最新版本中，循环不再使用任何数值分数作为停止、排序、best 选择或回退依据。每次运行都会固定执行 `--max-iters` 指定的轮数，默认是 5 轮。固定轮数结束后，系统会输出每一轮生成的视频和 shader；`final_*` 字段只是最后一轮结果的别名。

这次修改的核心原因是：当前任务不是简单追求某个差异值变小，而是验证“目标视频 -> 大模型差异审查 -> shader 修改 -> 新视频”的路径是否可行。对于翻转、拉伸、漩涡、强几何扭曲这类效果，数值分数很容易和真实视觉匹配程度不一致，因此不再把数值作为迭代控制信号。

## 二、整体流程

当前主流程在：

```text
effect_ir_pipeline/effect_ir/closed_loop_shader_iter.py
```

一次运行包含以下阶段。

### 2.1 目标视频生成 IR

输入目标视频后，系统先调用大模型生成 `target_ir`。这个 IR 描述的是视频中特效的变化机制，例如：

```text
region_scope
geometry_ops
appearance_ops
temporal_pattern
mask_dependency
strength
motion_hint
color_hint
geometry_pattern
summary
```

这里关注的不是画面内容，而是特效让画面发生了什么变化。

### 2.2 检索库中特效

系统用 `target_ir` 与库侧结构化 IR 做相似度匹配，选出 top1 特效作为初始参考：

```text
target_ir -> library_structured_ir_llm.jsonl -> selected_effect
```

第一版 shader 不是直接复制库里的 shader，而是：

```text
大模型参考 selected_effect 的源码/语义
结合 target_ir
重新生成一份 shader-lab 可运行 GLSL
```

库中 shader 提供的是语义锚点，大模型负责生成实际可渲染的初始版本。

### 2.3 渲染候选视频

每一轮都会生成：

```text
iter_xx_shader.md
iter_xx.vert.glsl
iter_xx.frag.glsl
iter_xx_rendered.mp4
```

渲染时使用目标视频第一帧作为输入图。shader 仍然需要满足：

```text
uProgress = 0 时输出原图
```

### 2.4 候选视频生成 IR

候选视频渲染后，系统会对候选视频生成 `candidate_ir`。这个 IR 用于给大模型提供候选视频的结构化描述，但不会被转换成数值分数，也不会参与 best 选择。

### 2.5 大模型视觉审查

从第二轮开始，系统会把目标视频、上一轮候选视频、当前 shader 和结构化 IR 一起交给大模型，让大模型指出差异并给出下一轮修改方案。

## 三、当前不再使用的内容

最新闭环中已经移除了以下机制：

```text
不再计算 target/candidate 的直接视觉距离
不再输出 latest_distance / best_distance
不再按数值判断 best iteration
不再因为某一轮更差而回退到历史 best
不再把 numeric_distance 传给大模型
不再把 observation/profile 数值轨迹传给大模型
不再把基于数值打分的 geometry primitive summary 传给大模型
```

也就是说，当前循环是线性的：

```text
iter_01 -> iter_02 -> iter_03 -> iter_04 -> iter_05 -> final
```

第 5 轮会作为 `final_*` 字段记录，但每一轮的结果都会完整保留并汇总输出，不存在“第 2 轮最好所以回退到第 2 轮”的逻辑。

## 四、大模型如何比较视频差异

虽然不使用数值分数，但仍然需要让大模型看到目标视频和候选视频之间的视觉差异。

系统会构造一张 contact sheet。每一行是同一个相对进度，每一行三列：

```text
TARGET | CANDIDATE | DIFF
```

其中：

```text
TARGET    目标视频帧
CANDIDATE 候选视频帧
DIFF      目标帧和候选帧的可视化差异图
```

这张图只作为大模型视觉审查材料，不参与循环停止、排序或 best 评选。

同时传给大模型的文本信息包括：

```text
primitive_vocabulary
primitive_edit_plan_schema
target_ir
candidate_ir
current_shader
```

不再传：

```text
numeric_distance
target_observation
candidate_observation
trajectory_summary
geometry_primitive_summary
```

## 五、结构化 primitive_edit_plan

为了避免大模型自由发挥，当前要求它输出结构化 `primitive_edit_plan`。

统一 primitive 词表包括：

```text
geometry:
identity, translate, scale_x, scale_y, rotate, flip_x, flip_y,
crop_or_pad, perspective_skew, radial_swirl, wave_warp

appearance:
brightness, contrast, saturation, rgb_shift, blur, sharpen, vignette

temporal_curves:
linear, ease_in, ease_out, ease_in_out, bell,
early_progressive, late_progressive, two_phase
```

模型必须从这个词表中选择下一轮要实现的操作，而不是输出开放式特效名称。

期望输出格式类似：

```json
{
  "visual_difference_summary": "候选视频相对目标视频的视觉差异",
  "evidence": [
    "引用 contact sheet、target_ir、candidate_ir 或 current_shader 中的证据"
  ],
  "candidate_errors": [
    "候选视频哪里太强、太弱或方向错误"
  ],
  "optimization_plan": [
    "下一轮应该如何修改 shader"
  ],
  "primitive_edit_plan": {
    "selected_primitives": [
      {
        "name": "scale_x",
        "role": "primary",
        "params": {
          "from": 1.0,
          "to": 1.8,
          "center": [0.5, 0.5],
          "axis": "x",
          "strength": "strong"
        },
        "temporal": "bell",
        "evidence": "具体视觉证据",
        "confidence": 0.8
      }
    ],
    "rejected_primitives": [
      {
        "name": "radial_swirl",
        "reason": "目标没有明显旋转/漩涡证据"
      }
    ],
    "implementation_notes": [
      "如何映射到 GLSL"
    ]
  },
  "operation_family": "small_geometry_adjustment",
  "change_family_allowed": false,
  "parameter_hints": {},
  "confidence": 0.0
}
```

系统会对这个 JSON 做基本规范化：

```text
selected_primitives 最多保留 3 个
primitive 名称必须来自统一词表
temporal 曲线必须来自统一词表
confidence 限制在 [0, 1]
如果 target_ir 明确没有几何变化，则过滤几何 primitive
```

## 六、如何生成下一轮 shader

下一轮 shader 生成 prompt 会包含：

```text
primitive_vocabulary
target_ir
candidate_ir
llm_visual_feedback
current_shader
selected_library_sources
```

其中最重要的是：

```text
llm_visual_feedback.parsed.primitive_edit_plan.selected_primitives
```

shader 生成模型需要：

```text
1. 按 selected_primitives 实现对应 GLSL 操作
2. 说明每个 primitive 如何映射到 shader 逻辑
3. 不要引入 primitive_edit_plan 没有选择的复杂形变
4. 输出两个 GLSL code block：vertex shader 和 fragment shader
```

fragment shader 的接口仍固定为：

```glsl
varying vec2 textureCoord;
uniform sampler2D inputImageTexture;
uniform float uProgress;
uniform float uTime;
```

不允许使用额外 uniform、外部纹理或 WebGL 不支持的语法。

## 七、固定 5 轮迭代逻辑

当前循环逻辑可以概括为：

```text
for iteration in 1..5:
    如果是第一轮:
        根据 selected_effect + target_ir 生成初始 shader
    否则:
        用大模型审查 target video 与上一轮 candidate video 的视觉差异
        得到 primitive_edit_plan
        根据 primitive_edit_plan 改写 shader

    渲染当前轮 shader 得到 iter_xx_rendered.mp4
    对当前轮候选视频生成 candidate_ir
    当前轮结果直接作为下一轮输入

final = 第 5 轮结果
```

这里没有任何数值比较，也没有历史 best。

如果某一轮出现生成失败，则这一轮不会计数。失败包括：

```text
shader 没有完整的两个 GLSL code block
vertex / fragment shader 缺少 main
fragment shader 缺少 inputImageTexture / uProgress / uTime
fragment shader 缺少 gl_FragColor 或 texture2D
shader 使用了额外 uniform
渲染失败
渲染视频不可读、空视频或黑帧
```

系统会把失败尝试记录到：

```text
failed_generation_attempts
```

然后重新生成同一轮，直到该轮产物通过检查，才会进入下一轮。默认每轮最多重试 3 次，可通过：

```text
--max-generation-attempts
```

控制。

## 八、输出结果

每次运行会输出：

```text
closed_loop_result.json
iter_01_shader.md
iter_01.vert.glsl
iter_01.frag.glsl
iter_01_rendered.mp4
...
iter_05_shader.md
iter_05.vert.glsl
iter_05.frag.glsl
iter_05_rendered.mp4
target_first_frame.png
```

`closed_loop_result.json` 中会额外提供每轮输出摘要：

```json
{
  "iteration_outputs": [
    {
      "iteration": 1,
      "rendered_video": "第 1 轮渲染视频",
      "shader_markdown": "第 1 轮 shader markdown",
      "vertex_shader": "第 1 轮 vertex shader",
      "fragment_shader": "第 1 轮 fragment shader"
    }
  ]
}
```

同时保留最后一轮的别名字段：

```json
{
  "final_candidate_video": "第 5 轮渲染视频",
  "final_shader_markdown": "第 5 轮 shader markdown",
  "final_shader_source": "第 5 轮 shader 源码"
}
```

不再包含：

```text
latest_distance
best_distance
best_iteration
best_candidate_video
```

## 九、当前结论

最新版本的闭环已经变成纯视觉审查驱动：

```text
固定 5 轮
不计算数值分数
不做 best 评选
不做回退
大模型根据 contact sheet 和 IR 判断差异
primitive_edit_plan 约束下一轮 shader 修改
每一轮结果都会输出，最后一轮只是 final 别名
```

这个版本更适合验证“通过大模型进行视觉差异审查并持续改写 shader”的主路径，也避免了数值分数对迭代方向的干扰。

## 十、下一步建议

后续重点不再是设计更复杂的数值距离，而是提升大模型视觉审查和 shader 改写之间的可控性。

可以继续推进：

```text
1. 让 primitive_edit_plan 更稳定，例如固定参数字段和时序字段
2. 为常见 primitive 提供模板化 GLSL 实现
3. 每轮要求模型明确说明上一轮视觉上哪里仍不匹配
4. 对 flip/stretch/swirl/wave 等 primitive 建立更标准的 shader 写法
5. 保留固定轮数，观察每一轮输出如何连续变化
```

当前最重要的原则是：

```text
不要再围绕某个数值目标优化
不要再用数值评选 best
让大模型负责视觉差异判断
让结构化 primitive 负责约束 shader 修改空间
```
## 十一、存在的问题
1. 局部特效识别不准，默认全屏特效
2. 默认第一帧为输入图，对某些特效不使用
