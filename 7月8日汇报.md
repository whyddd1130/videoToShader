# 7月8日汇报

## 一、项目目标

最终目标仍然是：

```text
输入原始图片 + 图片经某一视频特效渲染后的视频
逆向推理出该特效对应的 shader 源码
```

当前阶段先聚焦第一步：

```text
输入原始图 + 渲染视频
识别它与库中特效之间的对应关系
```

也就是说，我们现在做的不是普通分类，而是把“特效的变化机制”先表示出来，再用这个表示去做检索和对比。

## 二、IR 是怎么构造的

我们现在使用的是一个中间表示 IR，用来描述“特效对输入图做了什么变化”，而不是描述图像本身有什么内容。

IR 的构造遵循双侧统一表示的思路：

```text
库侧：代码 -> IR
查询侧：原始图 + 视频 -> IR
比较：查询 IR vs 库 IR
```

IR 主要包含以下几类信息：

| 字段 | 含义 |
|---|---|
| `summary` | 一句话概括这个特效的主要视觉变化 |
| `ops` | 主要操作类型，例如 blur、distortion、mask_matte、geometry_transform |
| `attributes` | 连续强度描述，例如颜色变化、模糊强度、畸变强度、运动强度 |
| `temporal` | 是否有时序变化，以及变化模式 |
| `tags` | 关键视觉概念，例如 bokeh、sphere、page_turn、warp |
| `controls` | 和 shader 参数或控制语义相关的描述 |

一个 IR 的核心目标不是复述输入图片内容，而是回答：

```text
这个特效让画面发生了什么变化？
变化是全局的还是局部的？
变化是静态的还是动态的？
主要是颜色、模糊、畸变、遮罩，还是几何变形？
```

### 2.1 库侧 IR

库侧只有代码、`filter.json`、Lua shader 等静态信息，没有库视频。

所以库侧 IR 的输入只能是代码相关信息，输出的是：

```text
代码语义 -> 视觉变化机制 IR
```

这一步的重点是把 shader 逻辑翻译成视觉层面的描述，例如：

```text
是否做了球面映射
是否做了局部遮罩
是否做了亮度/颜色调整
是否存在周期性动画
是否存在边缘增强或像素化
```

### 2.2 查询侧 IR

查询侧输入是：

```text
原始图 + 特效渲染后的视频
```

查询侧 IR 要强调的是“相对原图发生了什么变化”，不是描述画面里有什么物体。

例如，不应该只说：

```text
画面里有圆形和网格
```

而应该说：

```text
原图被做了球面变换，边缘发生拉伸，整体亮度有变化，局部区域存在遮罩，且变化随时间推进。
```

## 三、IR 如何双向用于特征对比

这里的“双向”主要有两层意思。

### 3.1 查询侧到库侧

这是最直接的检索方向：

```text
查询 IR -> 与库 IR 比较 -> 找最像的特效
```

这个过程的目标是把视频中的变化机制对齐到库中特效的变化机制上。

例如：

```text
查询 IR: 局部遮罩 + 亮度增强 + 周期性运动
库 IR:   局部遮罩 + 亮度增强 + 周期性运动
```

两者越接近，说明检索越可靠。

### 3.2 库侧到查询侧

第二层是反向解释：

```text
库 IR -> 解释查询 IR
```

它的作用不是直接做分类，而是帮助我们理解“为什么这个库特效会排在前面”。

这个方向可以用于：

```text
特征对齐分析
误差分析
结果可解释性输出
后续 shader 生成的条件约束
```

比如，如果库 IR 里有 `mask_dependency` 和 `motion_intensity`，而查询 IR 也有相近的变化，那么我们就可以说明：

```text
模型更关注的是变化机制的一致性，而不是输入图的语义内容。
```

## 四、把视频信息加入 IR 后有什么影响

我们后来在查询侧 IR 中加入了视频信息，具体包括帧间变化、运动强度、时间上的差分模式等。

这带来的影响主要有三点。

### 4.1 优点

第一，IR 对时序信息更敏感了。

以前只看静态帧，很多特效很像，容易混淆；加入视频后，模型更容易区分：

```text
静态模糊
周期闪烁
渐进式过渡
局部运动
几何扭曲
```

第二，IR 更聚焦“相对原图发生了什么变化”。

这会让查询侧表示更接近实际任务，因为我们最终不是识别物体，而是识别特效机制。

第三，结果不再容易出现“很多样本算出完全相同值”的情况。

加入视频变化后，表示更敏感，能更细地反映不同特效在不同图片上的差异。

### 4.2 局限

但视频信息也带来一个问题：

```text
如果依赖过多视频模板信息，容易偏向离线实验，不一定能部署。
```

尤其是库侧，我们实际拿不到“库特效在某个输入图上的渲染视频”，所以库侧 IR 不能依赖视频模板。

因此，目前更合理的方式是：

```text
查询侧可以充分利用视频信息；
库侧仍然坚持代码 -> IR；
最终比较仍然只在 IR 层完成。
```

### 4.3 当前结论

视频信息最重要的价值不是替代 IR，而是增强查询侧 IR 的质量。

可以把它理解成：

```text
视频信息帮助我们更准确地提炼“变化是什么”；
IR 则负责把这些变化固定成统一、可比较、可检索的表示。
```

## 五、现阶段效果的理解

从目前的实验来看，可以得出几个比较明确的判断：

1. 只做普通分类，不适合当前任务。
2. IR 方案是正确方向，因为它更接近“变化机制匹配”。
3. 加入视频信息后，查询侧的描述能力更强，特征更敏感。
4. 库侧仍然不能依赖视频模板，否则不符合实际部署约束。
5. 后续提升的关键，不是把 IR 无限做复杂，而是让 IR 更稳定、更聚焦“变化本身”。

## 六、下一步工作

下一步我认为可以从四个方向继续推进。

### 6.1 提升库侧 IR 的稳定性

让同一个特效无论由谁解析、由什么代码版本生成，IR 结构都尽量一致。

重点包括：

```text
统一字段格式
统一 op 类型映射
统一时序描述
统一强度表达
```

### 6.2 提升查询侧 IR 的质量

查询侧要更聚焦“相对原图发生了什么变化”，少描述场景内容，多描述变化机制。

例如更强调：

```text
模糊、遮罩、几何扭曲、边缘增强、颜色偏移、时间演化
```

而不是：

```text
画面里有什么对象、什么背景、什么颜色块
```

### 6.3 优化 IR 相似度计算

当前比较方式已经不只是单一字符串相似度，而是包含：

```text
structured_score
signature_score
diagnostic_score
```

后续可以进一步学习各个字段的权重，或者针对不同特效族使用不同的比较侧重点。

### 6.4 扩充数据并做更细的评测

当前 92 个特效的数据已经暴露出一些共性问题，比如同类特效之间容易混淆。

下一步可以补充：

```text
更多输入图
更多时序变化模式
更多同族但不同机制的特效
更多 hard negative 样本
```

这样可以让 IR 更有区分度，也能减少过拟合到少数现有特效。

## 七、总结

当前方案的核心结论是：

```text
IR 是连接“视频特效输入”与“shader 代码输出”的中间桥梁。
```

更具体地说：

```text
库侧用代码生成 IR；
查询侧用原图 + 视频生成 IR；
两边在 IR 层比较变化机制；
再根据匹配结果继续走向 shader 生成。
```

视频信息的加入提升了查询侧 IR 的表达能力，但库侧仍必须保持代码到 IR 的纯静态映射。

所以后续工作重点是：

```text
让 IR 更稳定
让查询侧 IR 更聚焦变化
让相似度更能反映机制差异
让检索结果更接近最终 shader 生成目标
```
