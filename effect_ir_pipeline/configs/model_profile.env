# K3 profile: source env.sh first, then source this file before running a baseline.
# K3 needs model-accessible HTTP image URLs; use the direct baseline URL arguments.
export EFFECT_IR_LLM_MODEL="ep-gn44so-1784714076968934949"
export EFFECT_IR_LLM_STREAM=1
export EFFECT_IR_LLM_MAX_TOKENS=6000
export EFFECT_IR_LLM_TIMEOUT=480
export EFFECT_IR_LLM_RETRIES=1
export EFFECT_IR_LLM_CONTENT_MODE=parts

# The code-side model remains the existing GPT endpoint from env.sh.
