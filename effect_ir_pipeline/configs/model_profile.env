# GPT profile: source env.sh first, then source this file before running a baseline.
# This retains the original direct baseline: local data-URL inputs, standard
# two-shader output, and no streaming requirement.
export EFFECT_IR_LLM_MODEL="ep-fipdyi-1784171757952297366"
export EFFECT_IR_LLM_STREAM=0
unset EFFECT_IR_LLM_MAX_TOKENS
export EFFECT_IR_LLM_TIMEOUT=300
export EFFECT_IR_LLM_RETRIES=3
export EFFECT_IR_LLM_CONTENT_MODE=parts
export EFFECT_IR_CODE_LLM_MODEL="ep-fipdyi-1784171757952297366"
